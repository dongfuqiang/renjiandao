# MTab 设置弹窗完整总结

## 一、设置弹窗整体结构

### 1. 弹窗布局
- 背景遮罩：黑色半透明，点击可关闭
- 主弹窗：白色背景，圆角8px，阴影，最大宽度900px，最大高度90vh
- 顶部栏：左侧标题"设置"，右侧关闭按钮

### 2. 左侧导航栏
- 用户信息区：显示"游客"头像和昵称
- 主要设置标签（4个）：
  - 常规设置
  - 主题标签
  - 壁纸设置
  - 时间日期
- 其他功能按钮（2个）：
  - 数据变动记录
  - 关于我们

---

## 二、各设置标签页详情

### 1. 常规设置

#### 功能开关
- **搜索新页面打开**：是否在新标签页打开搜索结果
- **标签新页面打开**：是否在新标签页打开书签
- **进入程序自动聚焦搜索**：是否自动聚焦到搜索框
- **搜索词联想功能**：是否显示搜索建议
- **搜索历史（仅本地生效）**：是否记录搜索历史
- **图标快捷搜索展示**：是否显示图标快捷搜索

#### 功能选择
- **快捷翻译**：选择翻译工具（Google翻译、百度翻译、有道翻译）

---

### 2. 主题标签

#### Dock栏和侧栏
- **开启Dock栏**：
  - 关闭：不显示Dock栏
  - 联动：与页面联动
  - 独立：独立显示
- **垃圾桶**：是否显示垃圾桶
- **保留上次查看的分类**：是否记住上次浏览的分类
- **页面首尾循环滚动**：页面滚动到首尾时是否循环
- **侧栏开关**：是否显示侧栏
- **侧栏自动隐藏**：侧栏是否自动隐藏
- **侧栏位置**：左侧/右侧
- **个人中心位置**：左侧/右侧

#### 图标和字体
- **图标字体颜色**：颜色选择器
- **图标背景**：是否显示图标背景
- **图标大小**：32px-128px可调节（默认64px）
- **图标圆角**：0px-20px可调节（默认8px）
- **图标列距**：8px-32px可调节（默认16px）
- **图标最大列数**：4列-16列可调节（默认8列）
- **标签Tooltip文字提示**：是否显示提示

---

### 3. 壁纸设置

#### 壁纸预览
- 当前壁纸预览
- 在线壁纸按钮
- 恢复默认按钮

#### 壁纸选择
- 4个预设壁纸选项，支持预览和切换

#### 背景效果
- **背景模糊值**：0px-20px可调节（默认10px）
- **遮罩透明度**：0%-100%可调节（默认70%）

---

### 4. 时间日期

#### 时间预览
- 显示实时时间和日期预览

#### 时间设置
- **显示时间**：是否显示时间
- **日期字体颜色**：颜色选择器（默认白色）

#### 内容展示
- **年月**：是否显示年月
- **秒**：是否显示秒
- **24小时制**：是否使用24小时制
- **周**：是否显示星期
- **农历**：是否显示农历
- **干支**：是否显示干支

---

## 三、默认配置

### 常规设置默认值
- 搜索新页面打开：true
- 标签新页面打开：true
- 进入程序自动聚焦搜索：true
- 搜索词联想功能：true
- 搜索历史（仅本地生效）：false
- 图标快捷搜索展示：true

### 主题标签默认值
- 开启Dock栏：联动
- 侧栏开关：true
- 侧栏自动隐藏：false
- 侧栏位置：左侧
- 个人中心位置：左侧
- 图标大小：64px
- 图标圆角：8px
- 图标列距：16px
- 图标最大列数：8列

### 壁纸设置默认值
- URL：https://images.unsplash.com/photo-1506748686214-e9df14d4d9d0
- 背景模糊值：10px
- 遮罩透明度：70%

### 时间日期默认值
- 显示时间：true
- 日期字体颜色：#ffffff
- 年月：true
- 秒：true
- 24小时制：true
- 周：true
- 农历：true
- 干支：true

---

## 四、技术实现特点

1. **状态管理**：使用Zustand进行状态管理，支持本地存储持久化
2. **组件化**：各设置标签页独立组件
3. **响应式**：使用Tailwind CSS实现响应式布局
4. **交互式**：开关按钮、滑块、颜色选择器等丰富交互
5. **用户体验**：实时预览、直观操作、优雅动画
