# MTAB 插件修复说明

## 🔴 必须修复的问题

### 一、CSS 修复

#### 1. 删除多余的闭合括号
**位置**：文件末尾

**错误代码**：
```css
.nav-list > ul {
    display: grid !important;
    ...
}






}  /* ← 这个括号是多余的！必须删除！ */

.tx-night .nav-list ul .set-item .data-name {
    color: rgba(255, 255, 255, 0.9) !important;
}
```

**修复后**：
```css
.nav-list > ul {
    display: grid !important;
    ...
}

.tx-night .nav-list ul .set-item .data-name {
    color: rgba(255, 255, 255, 0.9) !important;
}
```

---

#### 2. 修复背景颜色冲突

**问题**：有三处冲突的定义
```css
/* 第一处 - 强制白色 */
.nav-list ul .set-item .data-img {
    background: #ffffff !important;
}

/* 第二处 - 强制 none */
.nav-list ul .set-item .data-img {
    background: none !important;
}

/* 第三处 - 强制 inherit */
.nav-list ul .set-item .data-img {
    background: inherit !important;
}
```

**修复方案**：只保留支持 data-bg-color 的版本
```css
/* 删除所有冲突的定义，只保留： */
.nav-list ul .set-item .data-img {
    background-color: #ffffff !important;
}

/* 让 data-bg-color 行内样式生效 */
.nav-list ul .set-item[data-bg-color] .data-img {
    background-color: var(--data-bg-color, #ffffff) !important;
}

.tx-night .nav-list ul .set-item[data-bg-color] .data-img {
    background-color: var(--data-bg-color, rgba(255, 255, 255, 0.95)) !important;
}
```

---

### 二、JavaScript 修复

#### 1. 修复 switchCateWithAnimation 函数

**位置**：mtab.js 末尾附近

**错误代码**（重复/截断）：
```javascript
if (direction === 'up') {
    newMainInfo.style.transform = 'translateY(60px)';
} else {
    newMainInfo.style.transform = 'scale(1.05)';
}
newMainInfo.style.opacity = '0';
newMainInfo.offsetHeight;
newMainInfo.style.transition = 'all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94)';
newMainInfo.style.transform = 'translateY(0) scale(1)';
newMainInfo.style.opacity = '1';
    }
}


      newMainInfo.style.transform = 'scale(1.05)';  /* ← 重复代码，删除！ */
                    }
                    newMainInfo.style.opacity = '0';
                    newMainInfo.offsetHeight;
                    newMainInfo.style.transition = 'all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94)';
                    newMainInfo.style.transform = 'translateY(0) scale(1)';
                    newMainInfo.style.opacity = '1';
```

**修复后**：
```javascript
function switchCateWithAnimation(url, direction) {
    var mainInfo = document.querySelector('.main-info');
    if (mainInfo) {
        mainInfo.style.transition = 'all 0.25s ease-out';
        if (direction === 'down') {
            mainInfo.style.transform = 'translateY(-60px)';
        } else if (direction === 'up') {
            mainInfo.style.transform = 'translateY(60px)';
        } else {
            mainInfo.style.transform = 'scale(0.95)';
        }
        mainInfo.style.opacity = '0';

        setTimeout(function() {
            txpostajax(url);

            setTimeout(function() {
                var newMainInfo = document.querySelector('.main-info');
                if (newMainInfo) {
                    if (direction === 'down') {
                        newMainInfo.style.transform = 'translateY(60px)';
                    } else if (direction === 'up') {
                        newMainInfo.style.transform = 'translateY(-60px)';
                    } else {
                        newMainInfo.style.transform = 'scale(1.05)';
                    }
                    newMainInfo.style.opacity = '0';
                    newMainInfo.offsetHeight;
                    newMainInfo.style.transition = 'all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94)';
                    newMainInfo.style.transform = 'translateY(0) scale(1)';
                    newMainInfo.style.opacity = '1';
                }
            }, 80);
        }, 250);
    } else {
        txpostajax(url);
    }
}
```

---

#### 2. 修复滚轮事件，允许页面正常滚动

**位置**：mtab.js 末尾

**错误代码**：
```javascript
document.addEventListener('wheel', function(e) {
    if ($('.tx-pop').length > 0 || $('.set-on-box').length > 0) {
        return;
    }
    
    e.preventDefault();  /* ← 这个会阻止所有滚动！ */
    
    if (e.deltaY > 20) {
        switchCateByScroll('down');
    } else if (e.deltaY < -20) {
        switchCateByScroll('up');
    }
}, { passive: false });
```

**修复后**：
```javascript
document.addEventListener('wheel', function(e) {
    if ($('.tx-pop').length > 0 || $('.set-on-box').length > 0) {
        return;
    }
    
    var deltaThreshold = 80; // 增大阈值，只有大幅滚动才切换分类
    if (Math.abs(e.deltaY) > deltaThreshold) {
        e.preventDefault();
        if (e.deltaY > 0) {
            switchCateByScroll('down');
        } else if (e.deltaY < 0) {
            switchCateByScroll('up');
        }
    }
}, { passive: false });
```

---

### 三、HTML 修复

#### 1. 删除重复的 jQuery

**错误代码**：
```html
<script src="<?php echo $zbp->host; ?>zb_system/script/jquery-2.2.4.min.js"></script>
<!-- 这会导致与 ZBlog 自带的 jQuery-1.8.3 冲突！ -->
```

**修复后**：
```html
<!-- 删除手动引入的 jquery-2.2.4，使用 ZBlog 自带的 -->
```

---

#### 2. 删除重复的 mtab.js

**错误代码**：
```html
<script src="<?php echo $zbp->host; ?>zb_users/plugin/wx_nav/style/js/mtab.js?v=4.1"></script>
...
<script src="<?php echo $zbp->host; ?>zb_users/plugin/wx_nav/style/js/mtab.js?v=4.1"></script>
```

**修复后**：
```html
<!-- 只保留一处 -->
<script src="<?php echo $zbp->host; ?>zb_users/plugin/wx_nav/style/js/mtab.js?v=4.1"></script>
```

---

#### 3. 修复页面标题默认值

**错误代码**：
```php
<title><?php echo $zbp->Config('wx_nav')->title; ?></title>
```

**修复后**：
```php
<title><?php echo $zbp->Config('wx_nav')->title ?: '我的导航'; ?></title>
```

---

#### 4. 修复 avatar.png 404

**修复方案**：在 `style/img/` 目录下创建 avatar.png，或修改 HTML 使用默认头像

```html
<!-- 修改前 -->
<img src="<?php echo $zbp->host; ?>zb_users/plugin/wx_nav/style/img/avatar.png" onerror="this.src='data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 24 24%22><circle cx=%2212%22 cy=%2212%22 r=%2210%22 fill=%22%236366f1%22/><path fill=%22%23fff%22 d=%22M12 11c1.1 0 2-.9 2-2s-.9-2-2-2-2 .9-2 2 .9 2 2 2zm0 2c-2.21 0-4 1.79-4 4h8c0-2.21-1.79-4-4-4z%22/></svg>'">

<!-- 修复后 - 直接使用 SVG，不依赖图片文件 -->
<img src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24'%3E%3Ccircle cx='12' cy='12' r='10' fill='%236366f1'/%3E%3Cpath fill='%23fff' d='M12 11c1.1 0 2-.9 2-2s-.9-2-2-2-2 .9-2 2 .9 2 2 2zm0 2c-2.21 0-4 1.79-4 4h8c0-2.21-1.79-4-4-4z'/%3E%3C/svg%3E" onerror="this.src='data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 24 24%22%3E%3Ccircle cx=%2212%22 cy=%2212%22 r=%2210%22 fill=%22%236366f1%22/%3E%3Cpath fill=%22%23fff%22 d=%22M12 11c1.1 0 2-.9 2-2s-.9-2-2-2-2 .9-2 2 .9 2 2 2zm0 2c-2.21 0-4 1.79-4 4h8c0-2.21-1.79-4-4-4z%22/%3E%3C/svg%3E'">
```

---

## 📋 修复检查清单

- [ ] 1. CSS 中多余的 `}` 已删除
- [ ] 2. CSS 背景颜色冲突已修复
- [ ] 3. JS `switchCateWithAnimation` 函数已修复
- [ ] 4. JS 滚轮事件已修复
- [ ] 5. HTML 重复 jQuery 已删除
- [ ] 6. HTML 重复 mtab.js 已删除
- [ ] 7. HTML 页面标题默认值已添加
- [ ] 8. HTML avatar.png 404 已修复

---

## 🎯 优先级

| 优先级 | 项目 |
|--------|------|
| 🔴 立即 | CSS 多余括号 |
| 🔴 立即 | JS 滚轮事件 |
| 🔴 立即 | JS switchCateWithAnimation |
| 🟡 高 | HTML 重复 jQuery |
| 🟡 高 | HTML 重复 mtab.js |
| 🟡 中 | CSS 背景冲突 |
| 🟢 低 | 页面标题 |
| 🟢 低 | avatar.png |
